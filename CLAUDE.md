# JPReader — Claude Instructions

## Architecture Reference

`ARCHITECTURE.md` at the project root is the authoritative reference for this codebase.

**When answering questions** about features, business logic, data flows, APIs, components, or architecture: read `ARCHITECTURE.md` first, then supplement with live code if needed.

**When implementing or modifying features**: after the code changes are complete, update the relevant sections of `ARCHITECTURE.md` to reflect what changed — endpoints, methods, flows, models, etc. Do not leave the doc stale.
